Para compilar o arquivo main.c é usado:
gcc -pthread main.c -o main

Os arquivos Java foram criados e executados na IDE NetBeans, pois não conseguimos executá-los pelo terminal. O arquivo principal é o Main.java.

Deve ser executado o programa em C e depois o programa em Java.
O programa em C cria a FIFO e o programa em JAVA a destrói.
